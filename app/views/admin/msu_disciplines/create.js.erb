var empty = $("#empty-message");
var status = "<%= j flash[:error] %>";

console.log(status);

if (status == "ok") {
    if (empty) {
        empty.remove();
    }

    $('#disciplineModal').modal('hide');
    $('#disciplines').append("<%= j render(:partial => '/admin/msu_disciplines/discipline_menu_item', locals: { discipline: @discipline}) %>");

    // window.location.reload()
}
